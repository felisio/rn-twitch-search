import { StyleSheet, ViewStyle } from 'react-native';

interface __model__Styles {
  view: ViewStyle;
}

export const styles = StyleSheet.create<__model__Styles>({
  view: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  }
});
